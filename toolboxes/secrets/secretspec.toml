[project]
name = "secrets_toolbox"
revision = "1.0"
# Extend configurations from subdirectories
# extends = [ "subdir1", "subdir2" ]

[profiles.default]
TEST_SECRET = { description = "test secret in jn home-manager project", required = true }

[profiles.default.defaults]
providers = ["jessenieboer_home_manager"] # defined in global config in ~/.config/secretspec/config.toml

# [providers]
# project_secret_vault = { uri = "bws://project-key-here", credentials = { access_token = "project_access_token" } }
# project_access_token = "pass://path-to-machine-account-token" # name/path of machine account token stored locally in pass