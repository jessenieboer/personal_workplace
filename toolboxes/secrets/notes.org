requires
- bws to manage secrets
- a bws machine access token in pass


requires a ~/.config/secretspec/config.toml; generate with secretspec config init

(my-secretspec-get "XAI_API_KEY")


	  
(getenv "PATH")
(executable-find "secretspec")
