#+TITLE: Python Toolbox
#+OPTIONS: toc:nil num:nil
#+EXPORT_FILE_NAME: ../../README.md

#+html: <h1 align="center">Python Toolbox</h1>
#+html: <p align="center">Tools, settings, templates and practices for Python development.</p>

* What
** Features :noexport:
* Why
* For Whom :noexport:
* How :noexport:
** Requirements :noexport:
** Installation :noexport:
#+BEGIN_SRC yaml
inputs:
  llm-agents:
    url: github:numtide/llm-agents.nix/assets
  neolabhq-cek:
    flake: false
    url: github:NeoLabHQ/context-engineering-kit
  nixpkgs:
    url: github:NixOS/nixpkgs/nixpkgs-unstable
  nixpkgs-python:
    url: github:cachix/nixpkgs-python
    inputs:
      nixpkgs:
        follows: nixpkgs
  trailofbits-skills:
    flake: false
    url: github:trailofbits/skills
  wshobson-agents:
    flake: false
    url: github:wshobson/agents
    
imports:
  - /toolboxes/ai
  - /toolboxes/emacs
  - /toolboxes/git
  - /toolboxes/readme
  - /toolboxes/secrets
#+END_SRC
** Use :noexport:
*** Details
 - attribute ::
- behavior ::
- data ::
- defined ::
- definition ::
- iterable :: 
- method :: 
- module :: a unit of Python code
- namespace :: 
- object :: Any data with state (attributes or value) and defined behavior (methods).
- package :: A collection of modules
  - technically a module with a __path__ attribute
- project ::
- pyproject.toml :: file containing configurations for various tools
- sequence :: 
- value :: 
- variable ::
- venv :: a module that creates and manages virtual environments
- virtual environment :: a directory tree that contains a particular version of Python and additional packages
- workspace :: a collection of one or more packages that are managed together
  - should contain uv.lock and pyproject.toml
- workspace member :: a package in a workspace

**** Troubleshooting
**** License
** Development :noexport:
*** Process
*** Important Decisions
*** To Do
*** Contributing
* By Whom :noexport:
llm-agents: eca and opencode via nix
