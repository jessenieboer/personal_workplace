* project info
#+name: project_name
#+BEGIN_SRC emacs-lisp :tangle no :noweb-ref project_name
this_project_name
#+END_SRC

#+name: project_directory
#+BEGIN_SRC emacs-lisp :tangle no :noweb-ref project_directory
/path/to/this_project_name/
#+END_SRC

note: each path to an agenda file must be in double quotes, and paths must all be on a single line, no newlines
#+BEGIN_SRC emacs-lisp :tangle no :noweb-ref subproject_agenda_files

#+END_SRC

** project file generation
#+name: project_file
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
(let ((dir "<<project_directory>>")
      (name "<<project_name>>"))
  (let ((file (concat dir "project_management/" name ".org")))
    (if (not (file-exists-p file)) file "no")))
#+END_SRC

#+BEGIN_SRC org :tangle (org-sbe "project_file") :noweb tangle
,* Tasks
,* Occurances
,* References
,* Ideas
,* User Stories
#+END_SRC
