#+PROPERTY: header-args:emacs-lisp :tangle yes
* project config
I tangle this file to generate templates/dir-locals.el.in rather than editing that file directly, so i can use org to organize the lisp code nicely

must have this in emacs config for agenda to work, and adding it to this file requires eval,
(add-hook 'org-agenda-mode-hook 'hack-dir-local-variables-non-file-buffer)
** dir-locals
:PROPERTIES:
:ID:       30db7c2d-345a-4875-9b98-c2ee15c92282
:END:
#+BEGIN_SRC emacs-lisp :tangle ./templates/dir-locals.el.in :noweb tangle 
  ((nil . ((project-name . "@PROJECT_NAME@")
  	 (org-agenda-files . ("@PROJECT_MANAGEMENT_DIRECTORY@/@PROJECT_NAME@.org" @SUBPROJECT_MANAGEMENT_DIRECTORIES@))
  	 (org-agenda-custom-commands . (<<hard_dates_view>> <<initial_scheduling_view>> <<all_dependencies_view>> <<initial_dependencies_view>> <<strategic_evaluation_view>> <<tactical_evaluation_view>> <<effort_estimation_view>> <<activity_view>> <<basics_view>> <<focus_view>> <<calendar_view>> <<dependent_items_view>> <<done_items_view>> <<all_items_view>>))
  	 (org-archive-location . "%s_archive::")
  	 (org-archive-mark-done . t)
  	 (org-directory . "@PROJECT_MANAGEMENT_DIRECTORY@/")
  	 (org-capture-templates . (("s" "Task" entry
  				    (file+headline "@PROJECT_NAME@.org" "Tasks")
  				    <<task_capture_template>>)
  				   ("." "Task with due date" entry
  				    (file+headline "@PROJECT_NAME@.org" "Tasks")
  				    <<time_bound_task_capture_template>>)
  				   ("e" "Routine" entry
  				    (file+headline "@PROJECT_NAME@.org" "Tasks")
  				    <<routine_capture_template>>)
  				   (")" "Routine with due date" entry
  				    (file+headline "@PROJECT_NAME@.org" "Tasks")
  				    <<time_bound_routine_capture_template>>)
  				   ("n" "Occurance" entry
  				    (file+headline "@PROJECT_NAME@.org" "Occurances")
  				    <<occurance_capture_template>>)
  				   ("y" "Reference" entry
  				    (file+headline "@PROJECT_NAME@.org" "References")
  				    <<reference_capture_template>>)
  				   ("o" "User story" entry
  				    (file+headline "@PROJECT_NAME@.org" "User stories")
  				    <<task_capture_template>>)
  				   ("i" "Idea" item
  				    (file+headline "@PROJECT_NAME@.org" "Ideas")
  				    <<item_capture_template>> :immediate-finish t)))
  	 (org-deadline-warning-days . 0)
  	 ;; (org-priority-highest . 2)
  	 ;; (org-priority-default . 2)
  	 ;; (org-priority-lowest . 2)
  	 (org-global-properties . (("FREQUENCY_ALL" . "once repetitive continuous")
  				   ("HAS_HARD_START_DATE_ALL" . "no yes")
        				   ("HAS_HARD_DUE_DATE_ALL" . "no yes")
  				   ("HAS_SOFT_START_DATE_ALL" . "no yes")
        				   ("HAS_SOFT_DUE_DATE_ALL" . "no yes")
  				   ;; SCHEDULED functions as start date
        				   ;; DEADLINE functions as due date
  				   ("HARD_INTERNAL_DEPENDENCY_ALL" . "no yes")
  				   ("SOFT_INTERNAL_DEPENDENCY_ALL" . "no yes")
  				   ("HARD_EXTERNAL_DEPENDENCY_ALL" . "no yes")
  				   ("SOFT_EXTERNAL_DEPENDENCY_ALL" . "no yes")
  				   ("STRATEGIC_VALUE_ALL" . "less average more")
  				   ("TACTICAL_VALUE_ALL" . "less average more")
  				   ("ESTIMATED_EFFORT_ALL" . "less average more")
        				   ;; ("ESTIMATED_EFFORT_ALL" . "1h 4h 1d 2d 3d 5d 8d 13d 21d 34d 55d 89d 144d 233d")
        				   ("ESTIMATED_$_COST_ALL" . "trivial small medium large")
        				   ("ACTUAL_EFFORT")
        				   ;; ("ACTUAL_$_COST")
  				   )))))
#+END_SRC
			     
** capture templates
#+NAME: task_capture_template
#+BEGIN_SRC text :tangle no :noweb yes
"* future %^{Title}\n:PROPERTIES:\n:CATEGORY: @PROJECT_NAME@\n:FREQUENCY: once\n:HAS_HARD_START_DATE: no\n:HAS_HARD_DUE_DATE: no\n:HARD_INTERNAL_DEPENDENCY: \n:SOFT_INTERNAL_DEPENDENCY: \n:HARD_EXTERNAL_DEPENDENCY: \n:SOFT_EXTERNAL_DEPENDENCY: \n:STRATEGIC_VALUE: average\n:TACTICAL_VALUE: average\n:ESTIMATED_EFFORT: average\n:ESTIMATED_$_COST: trivial\n:ACTUAL_EFFORT: \n:ACTUAL_$_COST: \n:END:\n%?"
#+END_SRC

#+NAME: time_bound_task_capture_template
#+BEGIN_SRC text :tangle no :noweb yes
"* future %^{Title}\n:PROPERTIES:\n:CATEGORY: @PROJECT_NAME@\n:FREQUENCY: once\n:HAS_HARD_START_DATE: no\n:HAS_HARD_DUE_DATE: yes\n:HARD_INTERNAL_DEPENDENCY: \n:SOFT_INTERNAL_DEPENDENCY: \n:HARD_EXTERNAL_DEPENDENCY: \n:SOFT_EXTERNAL_DEPENDENCY: \n:STRATEGIC_VALUE: average\n:TACTICAL_VALUE: average\n:ESTIMATED_EFFORT: average\n:ESTIMATED_$_COST: trivial\n:ACTUAL_EFFORT: \n:ACTUAL_$_COST: \n:END:\n%?"
#+END_SRC

#+NAME: routine_capture_template
#+BEGIN_SRC text :tangle no :noweb yes
"* future %^{Title}\n:PROPERTIES:\n:CATEGORY: @PROJECT_NAME@\n:FREQUENCY: repetitive\n:HAS_HARD_START_DATE: no\n:HAS_HARD_DUE_DATE: no\n:HARD_INTERNAL_DEPENDENCY: \n:SOFT_INTERNAL_DEPENDENCY: \n:HARD_EXTERNAL_DEPENDENCY: \n:SOFT_EXTERNAL_DEPENDENCY: \n:STRATEGIC_VALUE: average\n:TACTICAL_VALUE: average\n:ESTIMATED_EFFORT: average\n:ESTIMATED_$_COST: trivial\n:ACTUAL_EFFORT: \n:ACTUAL_$_COST: \n:END:\n%?"
#+END_SRC

#+NAME: time_bound_routine_capture_template
#+BEGIN_SRC text :tangle no :noweb yes
"* future %^{Title}\n:PROPERTIES:\n:CATEGORY: @PROJECT_NAME@\n:FREQUENCY: repetitive\n:HAS_HARD_START_DATE: no\n:HAS_HARD_DUE_DATE: yes\n:HARD_INTERNAL_DEPENDENCY: \n:SOFT_INTERNAL_DEPENDENCY: \n:HARD_EXTERNAL_DEPENDENCY: \n:SOFT_EXTERNAL_DEPENDENCY: \n:STRATEGIC_VALUE: average\n:TACTICAL_VALUE: average\n:ESTIMATED_EFFORT: average\n:ESTIMATED_$_COST: trivial\n:ACTUAL_EFFORT: \n:ACTUAL_$_COST: \n:END:\n%?"
#+END_SRC

#+NAME: occurance_capture_template
#+BEGIN_SRC text :tangle no :noweb yes
"* future %^{Title}\n:PROPERTIES:\n:CATEGORY: @PROJECT_NAME@\n:END:\n%?"
#+END_SRC

#+NAME: reference_capture_template
#+BEGIN_SRC :tangle no :noweb yes
"* %^{Title}\n:PROPERTIES:\n:CATEGORY: @PROJECT_NAME@\n:END:\n%?"
#+END_SRC

#+NAME: item_capture_template
#+BEGIN_SRC :tangle no :noweb yes
"%^{Title}\n"
#+END_SRC

** agenda views
*** working views
#+NAME: hard_dates_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("\\" "Hard Dates" tags "HAS_HARD_START_DATE=\"\"|HAS_HARD_START_DATE=\"\"" ((org-overriding-columns-format "%7TODO(Status) %30ITEM(Task/Occurance) %HAS_HARD_START_DATE(Hard start?) %SCHEDULED(Start date) %HAS_HARD_DUE_DATE(Hard due?) %DEADLINE(Due date) %15CATEGORY(Project)") (org-agenda-overriding-header "Are there hard dates we need to be aware of?") (org-agenda-skip-function 'my-skip-references) (org-agenda-view-columns-initially t)))
#+END_SRC

#+NAME: initial_scheduling_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("w" "Initial Scheduling" tags "HAS_HARD_START_DATE=\"yes\"|HAS_HARD_DUE_DATE=\"yes\"|FREQUENCY=\"repetitive\"" ((org-overriding-columns-format "%7TODO(Status) %30ITEM(Task/Occurance) %HAS_HARD_START_DATE(Hard start?) %SCHEDULED(Start date) %HAS_HARD_DUE_DATE(Hard due?) %DEADLINE(Due date) %15CATEGORY(Project)") (org-agenda-overriding-header "What do we know is on the calendar?") (org-agenda-skip-function 'my-skip-references) (org-agenda-sorting-strategy '((tags deadline-up))) (org-agenda-view-columns-initially t)))
#+END_SRC

# #+NAME: soft_dates_view
# #+BEGIN_SRC emacs-lisp :tangle no :noweb yes
# ("\\" "Soft Dates" tags "HAS_SOFT_START_DATE=\"\"&HAS_SOFT_START_DATE=\"\"" ((org-overriding-columns-format "%7TODO(Status) %30ITEM(Task/Occurance) %HAS_SOFT_START_DATE(Soft start?) %SCHEDULED(Start date) %HAS_SOFT_DUE_DATE(Soft due?) %DEADLINE(Due date)") (org-agenda-overriding-header "Are there soft dates?") (org-agenda-skip-function 'my-skip-references) (org-agenda-view-columns-initially t)))
# #+END_SRC

# #+NAME: soft_scheduling_view
# #+BEGIN_SRC emacs-lisp :tangle no :noweb yes
# ("w" "Soft Scheduling" tags "HAS_SOFT_START_DATE=\"yes\"|HAS_SOFT_DUE_DATE=\"yes\"" ((org-overriding-columns-format "%7TODO(Status) %30ITEM(Task/Occurance) %HAS_SOFT_START_DATE(Soft start?) %SCHEDULED(Start date) %HAS_SOFT_DUE_DATE(Soft due?) %DEADLINE(Due date)") (org-agenda-overriding-header "What soft dates to we still need?") (org-agenda-skip-function 'my-skip-references) (org-agenda-sorting-strategy '((tags alpha-up))) (org-agenda-view-columns-initially t)))
# #+END_SRC

#+NAME: initial_dependencies_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("g" "Initial Dependencies" tags "HARD_INTERNAL_DEPENDENCY=\"\"&SOFT_INTERNAL_DEPENDENCY=\"\"HARD_EXTERNAL_DEPENDENCY=\"\"SOFT_EXTERNAL_DEPENDENCY=\"\"" ((org-overriding-columns-format "%7TODO(Status) %30ITEM(Task) %HARD_INTERNAL_DEPENDENCY(Hard Int) %SOFT_INTERNAL_DEPENDENCY(Soft Int) %HARD_EXTERNAL_DEPENDENCY(Hard Ext) %SOFT_EXTERNAL_DEPENDENCY(Soft Ext) %15CATEGORY(Project)") (org-agenda-overriding-header "Are there dependencies for these tasks?") (org-agenda-skip-function 'my-skip-non-tasks) (org-agenda-sorting-strategy '((tags alpha-up))) (org-agenda-view-columns-initially t)))
#+END_SRC               

#+NAME: all_dependencies_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("[" "All Dependencies" tags "*" ((org-overriding-columns-format "%7TODO(Status) %30ITEM(Task) %HARD_INTERNAL_DEPENDENCY(Hard Int) %SOFT_INTERNAL_DEPENDENCY(Soft Int) %HARD_EXTERNAL_DEPENDENCY(Hard Ext) %SOFT_EXTERNAL_DEPENDENCY(Soft Ext) %15CATEGORY(Project)") (org-agenda-overriding-header "Are there dependencies for these tasks?") (org-agenda-skip-function 'my-skip-non-tasks) (org-agenda-sorting-strategy '((tags alpha-up))) (org-agenda-view-columns-initially t)))
#+END_SRC

#+NAME: tactical_evaluation_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("l" "Tactial Evaluation" ((tags "FREQUENCY=\"once\"" ((org-agenda-cmp-user-defined 'my-org-cmp-tactical-value) (org-agenda-overriding-header "How beneficial would this task be if it were finished now?") (org-agenda-skip-function 'my-skip-non-tasks) (org-agenda-sorting-strategy '((tags user-defined-down))))) (tags "FREQUENCY=\"repetitive\"" ((org-agenda-cmp-user-defined 'my-org-cmp-tactical-value) (org-agenda-overriding-header "How beneficial would this routine be if it were finished now?") (org-agenda-skip-function 'my-skip-non-tasks) (org-agenda-sorting-strategy '((tags user-defined-down)))))) ((org-agenda-view-columns-initially t) (org-overriding-columns-format "%7TODO(Status) %30ITEM(Task) %TACTICAL_VALUE(Tac Val) %DEADLINE(Due date) %HAS_HARD_DUE_DATE(Hard due?) %15CATEGORY(Project)")))
#+END_SRC

#+NAME: strategic_evaluation_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("/" "Strategic Evaluation" tags "FREQUENCY=\"once\"" ((org-agenda-cmp-user-defined 'my-org-cmp-strategic-value) (org-overriding-columns-format "%7TODO(Status) %30ITEM(Task) %STRATEGIC_VALUE(Strat Val) %15CATEGORY(Project)") (org-agenda-overriding-header "How beneficial would this be if all tasks were finished?") (org-agenda-skip-function 'my-skip-non-tasks) (org-agenda-sorting-strategy '((tags user-defined-down))) (org-agenda-view-columns-initially t)))
#+END_SRC

#+NAME: effort_estimation_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("a" "Effort Estimation" tags "TACTICAL_VALUE<>\"less\"" ((org-agenda-cmp-user-defined 'my-org-cmp-estimated-effort) (org-overriding-columns-format "%7TODO(Status) %30ITEM(Tac Valuable, Doable Task) %ESTIMATED_EFFORT(Effort) %15CATEGORY(Project)") (org-agenda-overriding-header "How much of our effort would this take to finish?") (org-agenda-skip-function 'my-skip-all-dependent-tasks) (org-agenda-sorting-strategy '((tags user-defined-up))) (org-agenda-view-columns-initially t)))
#+END_SRC

# #+NAME: cost_estimation_view
# #+BEGIN_SRC emacs-lisp :tangle no :noweb yes
# ("r" "Cost Estimation" tags "*" ((org-overriding-columns-format "%7TODO(Status) %30ITEM(Task) %ESTIMATED_$_COST($ Cost)") (org-agenda-overriding-header "How much money would this cost to finish?") (org-agenda-skip-function 'my-skip-non-tasks) (org-agenda-view-columns-initially t)))
# #+END_SRC

# #+NAME: further_scheduling_view
# #+BEGIN_SRC emacs-lisp :tangle no :noweb yes
# (" " "Task Scheduling" tags "TACTICAL_VALUE<>\"less\"|DEADLINE=\"<tomorrow>\"" ((org-overriding-columns-format "%7TODO(Status) %30ITEM(Task) %TACTICAL_VALUE(Tac Val) %ESTIMATED_EFFORT(Effort)") (org-agenda-overriding-header "When are we doing this?") (org-agenda-skip-function 'my-skip-all-dependent-tasks) (org-agenda-view-columns-initially t)))
# #+END_SRC

#+NAME: activity_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("t" "Activity" tags "TACTICAL_VALUE<>\"less\"&DEADLINE=\"\"|DEADLINE<=\"<+2w>\"" ((org-agenda-cmp-user-defined 'my-org-cmp-estimated-effort) (org-overriding-columns-format "%7TODO(Status) %30ITEM(Task) %TACTICAL_VALUE(Tac Val) %ESTIMATED_EFFORT(Effort) %DEADLINE(Due date) %HAS_HARD_DUE_DATE(Hard due?) %15CATEGORY(Project)") (org-agenda-overriding-header "When are we doing these things?") (org-agenda-skip-function 'my-skip-all-dependent-tasks) (org-agenda-sorting-strategy '((tags todo-state-down))) (org-agenda-view-columns-initially t)))
#+END_SRC

# # resource_assignment
# #+NAME: resource_assignment_view
# #+BEGIN_SRC emacs-lisp :tangle no :noweb yes
# ("r" "Resource Assignment" tags "*" ((org-overriding-columns-format "%7TODO(Status) %30ITEM(Task) %TACTICAL_VALUE(Tac Val) %ESTIMATED_EFFORT(Effort)") (org-agenda-overriding-header "How much money would this cost to finish?") (org-agenda-skip-function ''my-skip-all-dependent-tasks) (org-agenda-view-columns-initially t)))
# #+END_SRC

#+NAME: basics_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("q" "Basics" tags "*" ((org-overriding-columns-format "%7TODO(Status) %30ITEM(Task) %FREQUENCY(Freq) %HAS_HARD_START_DATE(Hard start?) %HAS_HARD_DUE_DATE(Hard due?) %15CATEGORY(Project)") (org-agenda-overriding-header "What are the basics?") (org-agenda-skip-function 'my-skip-top-level-headlines) (org-agenda-view-columns-initially t)))
#+END_SRC

*** reporting views
#+NAME: focus_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("s" "Focus" tags "TODO=\"now\"" ((org-agenda-cmp-user-defined 'my-org-cmp-estimated-effort) (org-overriding-columns-format "%7TODO(Status) %30ITEM(Task) %15CATEGORY(Project)") (org-agenda-overriding-header "What are we focusing on right now?") (org-agenda-skip-function 'my-skip-non-tasks) (org-agenda-sorting-strategy '((tags user-defined-up))) (org-agenda-view-columns-initially t)))
#+END_SRC

#+NAME: calendar_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("e" "Calendar" agenda "SCHEDULED<>\"\"|DEADLINE<>\"\"" ((org-agenda-overriding-header "What's on the calendar?") (org-agenda-prefix-format '((agenda . ""))) (org-agenda-skip-function 'my-skip-top-level-headlines) (org-agenda-sorting-strategy '((tags deadline-up))) (org-agenda-span 'week) (org-agenda-use-time-grid nil)) ("@PROJECT_DIRECTORY@project/calendar.html" "@PROJECT_DIRECTORY@project/calendar.ps" "@PROJECT_DIRECTORY@project/calendar.ics"))
#+END_SRC

#+NAME: dependent_items_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("n" "Dependent Tasks" tags "HARD_INTERNAL_DEPENDENCY=\"yes\"|SOFT_INTERNAL_DEPENDENCY=\"yes\"|HARD_EXTERNAL_DEPENDENCY=\"yes\"|SOFT_EXTERNAL_DEPENDENCY=\"yes\"" ((org-overriding-columns-format "%7TODO(Status) %30ITEM(Task) %HARD_INTERNAL_DEPENDENCY(Hard Int) %SOFT_INTERNAL_DEPENDENCY(Soft Int) %HARD_EXTERNAL_DEPENDENCY(Hard Ext) %SOFT_EXTERNAL_DEPENDENCY(Soft Ext) %15CATEGORY(Project)") (org-agenda-overriding-header "What tasks have outstanding dependencies?") (org-agenda-skip-function 'my-skip-non-tasks) (org-agenda-sorting-strategy '((tags alpha-up))) (org-agenda-view-columns-initially t)))
#+END_SRC

#+NAME: done_items_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("c" "Done Items" tags "TODO=\"past\"" ((org-agenda-overriding-header "What have we done?") (org-agenda-prefix-format '((tags . ""))) (org-agenda-skip-function 'my-skip-top-level-headlines) (org-agenda-skip-function-global nil) (org-agenda-sorting-strategy '((tags alpha-up)))))
#+END_SRC

#+NAME: all_items_view
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
("," "All Items" tags "*" ((org-agenda-overriding-header "What are all the items in @PROJECT_NAME@?") (org-agenda-prefix-format '((tags . ""))) (org-agenda-skip-function 'my-skip-top-level-headlines) (org-agenda-sorting-strategy '((tags alpha-up)))) ("@PROJECT_DIRECTORY@project/test.html" "@PROJECT_DIRECTORY@project/test.ps" "@PROJECT_DIRECTORY@project/test.ics"))
#+END_SRC
